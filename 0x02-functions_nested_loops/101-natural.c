#include <stdio.h>
/**
 * main - print sum of multiples of three and five.
 * Return: Nothing.
*/
int main(void)
{
	int i, sum;

	for (i = 0; i < 1024; i++)
	{
		if (i % 3 == 0 || i % 5 == 0)
		{
			sum += i;
		}
	}
	printf("%d\n", sum);
	return (0);
}
