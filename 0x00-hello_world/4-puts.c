#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char myst[] = "\"Programming is like building a multilingual puzzle";

	puts(myst);
	return (0);
}
